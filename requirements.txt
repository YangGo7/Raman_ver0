# 필수 패키지 / Required packages
# 이미지 처리 / Image processing
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# 데이터 처리 / Data processing
pyyaml>=6.0
scipy>=1.11.0

# 시각화 / Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# 유틸리티 / Utilities
tqdm>=4.65.0

# 선택적 (GPU 가속) / Optional (GPU acceleration)
# opencv-contrib-python>=4.8.0
# scikit-image  # Windows 빌드 이슈로 주석 처리